server:
  port: 9001
spring:
  application:
    name: tensquare-user # 指定服务名称
  datasource:
    # url: jdbc:mysql://132.232.104.247:3306/tensquare_blog?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true
    # 不显式设置字符集，集成服务器配置，服务器的mysql已经设置为utf8mb4
    url: jdbc:mysql://132.232.104.247:3306/tensquare_user?useUnicode=true&useSSL=false&characterEncoding=utf8&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: '!QAZ2wsx'
    type: com.alibaba.druid.pool.DruidDataSource
    platform: mysql
  jpa:
    show-sql: true
    properties:
      hibernate:
        hbm2ddl:
          auto: update
  redis:
    host: 132.232.104.247
    port: 6379
    password: admin # 默认为空
    database: 0 # 默认也是使用 database=0
#jwt配置
jwt:
  config:
    key: tangsong
    ttl: 3600000
# eureka
eureka:
  client:
    service-url:
      defaultZone: http://140.143.237.191:6868/eureka/
  instance: # 开启ip注册，不开启，则使用服务名称注册
    prefer-ip-address: true
